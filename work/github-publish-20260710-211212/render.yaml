services:
  - type: web
    name: pcfix-backend
    runtime: node
    plan: free
    rootDir: outputs/production/no-npm-backend
    buildCommand: ""
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true

  - type: web
    name: pcfix-sistema
    runtime: static
    rootDir: outputs
    buildCommand: ""
    staticPublishPath: .
