services:
  - type: web
    name: pcfix-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/server-postgres.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        value: admin@pcfix.local
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        sync: false

  - type: web
    name: pcfix-sistema
    runtime: static
    rootDir: .
    buildCommand: ""
    staticPublishPath: .
